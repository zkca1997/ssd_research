TABLE OF CONTENTS:

classifier:		directory that contains classifier objects (a reduction technique paired with a classifer)
features:		directory that contains "GenerateFeatureMatrix.m" and feature generation methods (currently named with the convention "FeaturesX.m" where X is a capitalized letter of the alphabet
preprocessing:	directory that contains functions for preprocessing raw signal files prior to feature creation
reduction:		directory that contains feature reduction algorithms